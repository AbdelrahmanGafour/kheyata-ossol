# دليل الهوية البصرية — متجر "للخياطة أصول"

مستخلص من تحليل تصميم [themestate.com/demo/tailored](https://themestate.com/demo/tailored) (أسلوب فخم بسيط/Luxury Minimalist) مع تطبيق الألوان المأخوذة من شعار المتجر (`KH_Logo.webp`).

---

## 1. مصدر الإلهام

**من themestate/tailored (البنية والأسلوب):**
- طابع "فخامة بسيطة": مساحات بيضاء واسعة، عناصر لا تتزاحم، تركيز على القصة الحرفية (الصنعة، الدقة، الإتقان).
- عناوين كبيرة جريئة + نص وصفي أخف وزنًا تحتها.
- بطاقات خدمة/فئة تجمع أيقونة + عنوان + وصف قصير.
- قسم Hero يحمل شعارًا رئيسيًا + إحصائيات مصداقية (سنوات خبرة، عدد المنتجات، رضا العملاء).
- خطوات مرقّمة (1-2-3-4) لشرح رحلة العميل.
- فواصل أفقية رفيعة بين الأقسام، وأزرار نصية/محاطة بإطار أكثر من الأزرار المصمتة الثقيلة.
- شارات فئات (Category badges) وبطاقات مقالات/منتجات بصورة أعلى ونص أسفل.

**من الشعار (الألوان):** دائرة كريمية فاتحة، إطار كحلي ملكي غامق، نص كحلي داكن، لمسات ذهبية (خيط الإبرة، الخطوط، النقاط الفاصلة)، شارة توثيق زرقاء. هذا يترجم مباشرة إلى هوية "حرفية فاخرة" شرقية: **كحلي ملكي + كريمي عاجي + ذهبي نحاسي**.

---

## 2. لوحة الألوان (Color Palette)

| الاسم | الاستخدام | القيمة HEX | متغير CSS |
|---|---|---|---|
| كحلي ملكي (أساسي) | الهيدر، الأزرار الأساسية، العناوين المهمة | `#1B4C8C` | `--color-primary` |
| كحلي ملكي غامق | Hover، التدرجات، الفوتر | `#0F3164` | `--color-primary-dark` |
| كحلي فاتح | خلفيات فرعية، حدود فاتحة على الأزرق | `#3A6DB5` | `--color-primary-light` |
| كحلي داكن جدًا (نص) | العناوين والنصوص الأساسية | `#16233B` | `--color-text` |
| ذهبي نحاسي (مميز) | تفاصيل، أيقونات، فواصل، Badges | `#C9A24B` | `--color-accent` |
| ذهبي فاتح | خلفيات شارات، Hover خفيف | `#E8D5A0` | `--color-accent-light` |
| ذهبي غامق | نص على خلفية فاتحة، حدود | `#82631E` | `--color-accent-dark` |
| كريمي عاجي (خلفية أساسية) | خلفية الصفحة | `#FBF7EC` | `--color-bg` |
| كريمي أغمق (خلفية بديلة) | أقسام متبادلة، بطاقات | `#F2E9D4` | `--color-bg-alt` |
| أبيض | بطاقات، الهيدر عند التمرير | `#FFFFFF` | `--color-white` |
| رمادي نص ثانوي | نصوص وصفية | `#5B6472` | `--color-text-muted` |
| أخضر واتساب | زر تواصل واتساب | `#25D366` | `--color-whatsapp` |
| أحمر تنبيه | أخطاء، عدد السلة | `#C0392B` | `--color-danger` |

**قاعدة الاستخدام:** الكحلي هو لون الثقة والهوية (٦٠٪ من الاستخدام: هيدر، أزرار أساسية، عناوين)، الذهبي لون التمييز والفخامة (استخدام محدود ومركّز: خطوط، أيقونات، تفاصيل، حالة Hover)، الكريمي هو الخلفية المريحة التي تجعل الأزرق والذهبي يبرزان.

> **ملاحظة تباين:** `--color-accent-dark` مضبوط عمدًا على `#82631E` (وليس الذهبي الأفتح `#9C7B2E`) لأن هذا اللون يُستخدم كنص صغير (Eyebrow، تصنيف المنتج، التقييم) — والقيمة الأفتح كانت تعطي تباينًا 3.7:1 فقط أمام الخلفية الكريمية، أقل من حد WCAG AA (4.5:1 للنص العادي). لا تُرجع هذه القيمة للأفتح إلا إذا رُفع حجم/وزن النص إلى "نص كبير" حسب تعريف WCAG (≥24px عادي أو ≥18.7px عريض).

---

## 3. الطباعة (Typography) — عربي فقط

| الاستخدام | الخط | من Google Fonts | الأوزان |
|---|---|---|---|
| العناوين (Headings, Hero, أسماء الأقسام) | **El Messiri** | نعم — طابع نصف هندسي أنيق يعطي إحساس الفخامة والحرفية | 500, 600, 700 |
| النصوص (فقرات، أزرار، نماذج، قوائم) | **Tajawal** | نعم — خط عربي حديث عالي القراءة | 400, 500, 700, 800 |
| الأرقام المضبوطة (أسعار، عدّادات، تقييمات، شارة السلة) | **DM Sans** | نعم — خط هندسي دقيق يعطي الأرقام إحساسًا "منظّمًا" أقرب لواجهات القياس، بينما تتراجع الحروف العربية المجاورة (مثل "ج.م") تلقائيًا لخط Tajawal ضمن نفس العنصر | 500, 700 |

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=El+Messiri:wght@500;600;700&family=Tajawal:wght@400;500;700;800&family=DM+Sans:wght@500;700&display=swap" rel="stylesheet">
```

استخدم `var(--font-numeric)` (يحلّ إلى `'DM Sans', 'Tajawal', sans-serif`) فقط على عناصر محتواها أرقام في الغالب. لا تستخدمه على عناوين أو فقرات عربية كاملة.

**السلم الطباعي (Type Scale):**
- H1 (Hero): 44px / موبايل 32px — El Messiri 700
- H2 (عناوين أقسام): 32px / موبايل 26px — El Messiri 600
- H3 (عناوين بطاقات): 20px — El Messiri 600
- نص أساسي (Body): 16px — Tajawal 400، ارتفاع سطر 1.8
- نص صغير (Meta/وصف): 14px — Tajawal 400
- أزرار: 16px — Tajawal 700

الاتجاه: **RTL كامل** (`dir="rtl" lang="ar"`) لكل الصفحات.

---

## 4. التباعد والتخطيط (Spacing & Layout)

- نظام تباعد بمضاعفات 8px: `4, 8, 16, 24, 32, 48, 64, 96`
- عرض المحتوى الأقصى: `1280px` مع حشو جانبي `24px` (موبايل) / `48px` (ديسكتوب)
- تباعد سخي بين الأقسام: `96px` ديسكتوب، `56px` موبايل — تمامًا كما في الموقع المرجعي
- فواصل أفقية رفيعة (1px، لون `--color-accent` بشفافية 30٪) بين بعض الأقسام بدلاً من خطوط ثقيلة

---

## 5. الأزرار (Buttons)

- **زر أساسي (Primary):** خلفية `--color-primary`، نص أبيض، `border-radius: 8px`، عند Hover تتحول الخلفية إلى `--color-primary-dark` مع رفعة خفيفة (translateY -2px) وظل.
- **زر ذهبي (Accent/CTA مهم):** خلفية `--color-accent`، نص `--color-text`، يستخدم لأزرار "أضف للسلة" و"اطلب الآن".
- **زر إطار (Outline):** خلفية شفافة، حدود 1.5px بلون أساسي، نص بنفس اللون — يستخدم للأزرار الثانوية مثل "تعرف أكثر" (مطابقة لأسلوب الموقع المرجعي في الأزرار النصية/المحاطة).
- **زر نصي بسهم:** نص + أيقونة سهم صغيرة، تتحرك الأيقونة عند Hover — يستخدم داخل البطاقات ("اقرأ المزيد").
- زوايا دائرية متوسطة `8px-12px` (ليست حادة تمامًا وليست كبسولة كاملة) لإحساس معاصر أنيق.

---

## 6. البطاقات (Cards)

- خلفية بيضاء أو كريمية فاتحة، `border-radius: 16px`، ظل ناعم جدًا `0 4px 20px rgba(22,35,59,0.06)`.
- عند Hover: رفعة خفيفة + ظل أوضح + حدود ذهبية رفيعة تظهر تدريجيًا.
- بطاقة المنتج: صورة (نسبة 4:5) أعلى، اسم المنتج، السعر بخط عريض بلون كحلي، زر "أضف للسلة" ذهبي صغير.
- بطاقة خدمة/فئة: أيقونة دائرية بخلفية كريمية وحدود ذهبية رفيعة، عنوان، وصف سطرين.

---

## 7. الأيقونات

- **Font Awesome** (نمط Solid بشكل أساسي، Brands لأيقونات التواصل).
- أيقونات ذات علاقة بالخياطة: `fa-scissors` (مقص)، `fa-ruler` / `fa-ruler-combined` (مسطرة قياس)، `fa-shirt` (قماش/ملابس)، `fa-vest-patches`، `fa-thumbtack` (دبابيس)، `fa-palette`، `fa-tape` غير متوفر → استخدام `fa-ruler-horizontal` بديلاً لشريط القياس، `fa-hands` / `fa-hand-holding-heart` للدعم الفني، `fa-graduation-cap` للأكاديمية، `fa-truck-fast` للشحن، `fa-shield-halved` للضمان، `fa-phone` و`fa-whatsapp` (Brand) للتواصل، `fa-user` و`fa-lock` للحساب، `fa-cart-shopping` للسلة.

---

## 8. الحركة والتفاعل (Motion)

- **GSAP + ScrollTrigger:** الكشف التدريجي للعناصر عند التمرير (fade + translateY)، تحريك الأرقام الإحصائية (Counter)، تأثيرات Hover على البطاقات، Timeline لعناصر الهيدر عند التحميل.
- **anime.js (v4):** تأثير Parallax/Morph بين قسم الـ Hero وما يليه عند أول تمرير (تصغير + تعتيم + انزياح لطبقة الـ Hero لتكشف القسم التالي بحركة سلسة)، حركات دقيقة على الأيقونات (اهتزاز خفيف لإبرة الخياطة، دوران المقص عند hover).
- الحركة دائمًا **خفيفة وسريعة** (250-600ms)، Easing من نوع `power2.out` / `easeOutQuad` — لا مبالغة، تخدم الإحساس الفخم الهادئ لا الصاخب.

---

## 9. الأسلوب العام

**فخامة حرفية شرقية (Arabic Heritage Luxury):** نفس هدوء ورقي الموقع المرجعي (Luxury Minimalist)، لكن بدل الألوان المحايدة الغربية نستخدم الكحلي الملكي والذهبي المستوحيين من هوية "للخياطة أصول" — إحساس متجر متخصص وموثوق (خبرة +5 سنوات، أدوات أصلية، دعم فني حقيقي) لا متجر عام.

---

## 10. إمكانية الوصول (Accessibility)

- **حلقة تركيز ذهبية إلزامية:** كل عنصر تفاعلي (`a`, `button`, `input`, `select`, `textarea`, أي عنصر بـ`tabindex`) يظهر عليه `box-shadow: var(--focus-ring)` عند `:focus-visible` — حلقة ذهبية واضحة تتبع شكل العنصر (دائرية على الأزرار الدائرية، بزوايا على البطاقات). لا تُخفَ حلقة التركيز أبدًا (`outline: none` بدون بديل مرئي ممنوع).
- **حالة الضغط (`:active`):** كل زر/أيقونة تفاعلية (`.btn`, `.icon-btn`, `.add-cart-btn`, `.carousel-arrow`) لها تأثير `scale` بسيط عند الضغط لإحساس ملموس بالاستجابة.
- **تباين النص:** أي لون نص جديد يُختبر أمام الخلفية الفعلية التي سيظهر عليها (وليس بشكل نظري) ويجب ألا يقل عن 4.5:1 للنص العادي أو 3:1 للنص الكبير (≥24px عادي أو ≥18.7px عريض)، حسب WCAG 2.2 AA.
